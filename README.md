# people-fluent-exercise
# Run the following to exercise the code within the project
   mvn clean test
# The surefire plugin will generate reports on the status of the test run in target/surefire-reports
